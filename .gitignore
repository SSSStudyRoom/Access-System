# clasp（プロジェクトローカルに認証情報ファイルが作られた場合の保険。
# 通常は ~/.clasprc.json に保存されるためここには生成されないが、
# 誤って作られても絶対にコミットしないようにする）
.clasprc.json

# Node（将来 npm パッケージをローカルに入れた場合の保険）
node_modules/

# OS / エディタ
.DS_Store
Thumbs.db
